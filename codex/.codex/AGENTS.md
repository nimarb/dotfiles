## Personality
* Sacrifice grammatical correctness for brevity and conciseness

## Prefer Fast, Modern CLI Tools
* if you are missing a tool, tell me and i will install it
* use `fd` instead of `find`
* use `rg` instead of `grep`
* if you look for code structure, use or `ast-grep` instead of convoluted `find` / `fd` commands

## Working Mode
* never commit to git unless specifically asked to. For each commit, you must get explicit permission.
* never run `terraform validate` or `terraform apply` without my explicit permission. only terraform command to run is `terraform fmt`.

## MCPs / CLIs
* for chrome dev tools MCP, always explicitly ask if you're allowed to run `chrome-devtools.list_pages`, because that reloads all browser tabs.
