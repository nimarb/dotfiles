[
  {
    "context": "Editor && vim_mode == insert",
    "bindings": {
      // put key-bindings here if you want them to work in insert mode
      "j j": "vim::NormalBefore" // remap jj in insert mode to escape.
      // alternatives
      // "j j": ["vim::SwitchMode", "Normal"]  // also from docs
      // "j j": ["workspace::SendKeystrokes", "escape"]  // also closes any menus, source discord
    }
  },
  {
    "context": "Editor",
    "bindings": {
      "cmd-l": "workspace::ToggleRightDock"
    }
  },
  {
    "context": "Editor && showing_completions",
    "bindings": {
      "alt-space": "editor::AcceptEditPrediction"
    }
  }
  // {
  //   "context": "Editor && edit_prediction_conflict",
  //   "bindings": {
  //     // https://github.com/zed-industries/zed/discussions/24980
  //     // Here we bind tab to accept even when there's a language server completion
  //     // "tab": "editor::AcceptEditPrediction"
  //   }
  // }

  //{
  //  // use tab to cycle through completion suggestions
  //  "context": "Editor && showing_completions",
  //  "bindings": {
  //    "enter": "editor::ConfirmCompletion",
  //    "shift-tab": "editor::ContextMenuPrev",
  //    "tab": "editor::ContextMenuNext"
  //  }
  //}
]
