{
  "cleanupPeriodDays": 365,
  "statusLine": {
    "type": "command",
    "command": "input=$(cat); current_dir=$(echo \"$input\" | jq -r '.workspace.current_dir // .cwd'); branch=$(git -C \"$current_dir\" branch --show-current 2>/dev/null || echo \"no-git\"); printf \"\\033[2m%s: [%s]\\033[0m\" \"$current_dir\" \"$branch\""
  },
  "alwaysThinkingEnabled": true,
  "feedbackSurveyState": {
    "lastShownTime": 1753959093104
  }
}
